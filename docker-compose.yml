version: "3.9"

services:
  payments-saga-service:
    image: ${PAYMENTS_SAGA_IMAGE:-ghcr.io/jhromeroabx/rt-tcs-saga-teamokaren:latest}
    container_name: payments-saga-service
    ports:
      - "8080:8080"
    env_file:
      - .env.sample